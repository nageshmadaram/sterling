# Upper bounds, not just floors. An unpinned floor let CI resolve fastapi
# 0.141.1 / starlette 1.6.0 while a developer venv held 0.136.1 / 1.0.0, and
# the two versions disagree about what `include_router` puts in app.routes.
# Tested pair: fastapi 0.141.1 + starlette 1.6.0 (CI) and 0.136.1 + 1.0.0.
fastapi>=0.115.0,<0.142.0
starlette>=1.0.0,<1.7.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
numpy>=1.26.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0
scipy>=1.12.0
freezegun>=1.4.0
smartmoneyconcepts>=0.0.27
ta>=0.11.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0
cryptography>=42.0.0
websockets>=12.0
pyarrow>=18.0.0
pyjwt>=2.8.0

# Live evidence acceptance harness (study/snapback_live_evidence_acceptance.py):
# Kite login + FULL-mode WebSocket capture. Not used by the API at runtime.
kiteconnect
